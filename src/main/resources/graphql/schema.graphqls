
type Counter {
    count: Int
}

type Query {
    currentCount: Counter
}

type Mutation {
    setCount(count: Int!): Counter
    incrementCount: Counter
}

type Subscription {
    counter: Counter
}